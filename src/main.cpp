#include <iostream>
#include "lib/shell.h"

int main() {
    
    /*

    TODO:

    Get qcow2 path
    Mount it with qemu-nbd
    Show partitions of nbd device
    Select partition to write
    Select disk and partition to write to
    Write partition to disk
    Unmount nbd device
    Unmount physical partition
    Unmount qcow2 image

    */
    return 0;
}
